{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/cristhiandaviddemoyallanos/monastery-web\" && if [ -n \"$(git status --porcelain)\" ]; then git add -A && git commit -m \"auto: sync $(date '+%Y-%m-%d %H:%M')\" && git push origin main; fi 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Sincronizando cambios con GitHub..."
          }
        ]
      }
    ]
  }
}
